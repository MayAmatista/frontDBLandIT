import React from "react";
import { CoursesList } from "../components/coursesList";


export const Courses = (props) => {
    //nav con filtros
    //botón cerrar sesión
    //botón nuevo curso
    //lista de cursos <coursesList>
    return (
        <main>
            <CoursesList/>
        </main>
    );
}
