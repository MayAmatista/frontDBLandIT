import React from "react";
import { StudentsList } from "../components/studentstList";


export const Students = (props) => {
    //botón estrella (destacado)
    //botón agregar est 
    //lista de estudiantes <StudentsList>
    return (
        <main>
            <StudentsList/>
        </main>
    );
}